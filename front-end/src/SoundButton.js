import React, { Component } from 'react'

// Represents a button in the sound board
class SoundBoard extends Component {
    render() {
        return(<button>
               </button>
            )
    }
}