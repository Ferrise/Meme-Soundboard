import React, { Component } from 'react'

// Represents the page where a user can test a sound board
class SoundTest extends Component {
    render() {
        return(<body>
               </body>
            )
    }
}