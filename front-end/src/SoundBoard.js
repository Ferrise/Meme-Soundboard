import React, { Component } from 'react'

// Represents the sound board that the user will interact with
class SoundBoard extends Component {
    render() {
        return(<div>
               </div>
            )
    }
}